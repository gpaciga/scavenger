const Scavenger = require('../src/scavenger');

describe('Scavenger', () => {

    it('is a function', () => {
        expect(Scavenger).toEqual(expect.any(Function));
    });

    describe('initialization', () => {

    });
});
